compile code by using the following gcc compiler command:
type in terminal "gcc filename.c -lcurses".
on success compilation, the gcc compiler produces an executable file named
"a.out".
which will execute using the following command to view the output of program:
run it with 
"./a.out".
added ncurses.h.
created on 1440 x 900 (16:10) display resolution.
auto cleaning terminal after app is lauched.
