compile code by using the following gcc compiler command:
type in terminal "sudo gcc filename.c -o filename.out " | add "-lcurses" argument for <ncurses.h> lib.
on success compilation, the gcc compiler produces an executable file named: "filename.out".
which will execute using the following command to view the output of program: "./filename.out".
